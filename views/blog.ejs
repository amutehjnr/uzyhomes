<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="UZYHOMES Blog - Inspiration and insights for quiet luxury living, interior design, and thoughtful home curation.">
    <title><%= title %></title>
    
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- AOS Animation -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css">
    
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/images/favicon.svg">
    
    <style>
        /* ===== BLOG PAGE STYLES ===== */
        :root {
            --primary: #A68A6F;
            --dark: #2D2D2D;
            --light: #FAF9F7;
            --sand: #E8E4DD;
            --grey: #777777;
            --white: #FFFFFF;
            --transition: all 0.3s ease;
        }

        body {
            font-family: 'Inter', sans-serif;
            color: var(--dark);
            background-color: var(--light);
            overflow-x: hidden;
        }

        /* Alert Messages */
        .alert {
            padding: 1rem;
            margin-bottom: 2rem;
            border: 1px solid transparent;
            text-align: center;
        }
        
        .alert-success {
            background: #d4edda;
            color: #155724;
            border-color: #c3e6cb;
        }
        
        .alert-error {
            background: #f8d7da;
            color: #721c24;
            border-color: #f5c6cb;
        }

        /* Blog Hero */
        .blog-hero {
            padding: 8rem 0 4rem;
            background: linear-gradient(135deg, var(--light) 0%, var(--white) 100%);
            text-align: center;
        }

        .blog-hero h1 {
            font-family: 'Playfair Display', serif;
            font-size: 3.5rem;
            font-weight: 400;
            color: var(--dark);
            margin-bottom: 1rem;
        }

        .blog-hero p {
            font-size: 1.2rem;
            color: var(--grey);
            max-width: 600px;
            margin: 0 auto;
        }

        /* Category Filter */
        .blog-categories {
            padding: 2rem 0;
            background: var(--white);
            border-bottom: 1px solid var(--sand);
        }

        .category-filter {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 1rem;
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .category-filter li a {
            display: block;
            padding: 0.5rem 1.5rem;
            color: var(--grey);
            text-decoration: none;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: var(--transition);
            border: 1px solid transparent;
        }

        .category-filter li a:hover,
        .category-filter li a.active {
            color: var(--primary);
            border-color: var(--sand);
        }

        /* Featured Post */
        .featured-post {
            padding: 4rem 0;
            background: var(--white);
        }

        .featured-card {
            display: block;
            text-decoration: none;
            color: inherit;
            background: var(--light);
            transition: var(--transition);
            border: 1px solid var(--sand);
        }

        .featured-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(0,0,0,0.05);
        }

        .featured-image {
            height: 450px;
            overflow: hidden;
        }

        .featured-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: var(--transition);
        }

        .featured-card:hover .featured-image img {
            transform: scale(1.05);
        }

        .featured-content {
            padding: 3rem;
            background: var(--white);
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .featured-meta {
            display: flex;
            gap: 2rem;
            margin-bottom: 1rem;
            font-size: 0.85rem;
            color: var(--grey);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .featured-category {
            color: var(--primary);
        }

        .featured-content h2 {
            font-family: 'Playfair Display', serif;
            font-size: 2.2rem;
            font-weight: 400;
            color: var(--dark);
            margin-bottom: 1rem;
            line-height: 1.3;
        }

        .featured-excerpt {
            font-size: 1.1rem;
            color: var(--grey);
            margin-bottom: 2rem;
            line-height: 1.8;
        }

        .read-more {
            color: var(--primary);
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            text-decoration: none;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .read-more:hover {
            color: var(--dark);
            gap: 1rem;
        }

        /* Blog Grid */
        .blog-grid-section {
            padding: 4rem 0;
            background: var(--light);
        }

        .blog-card {
            display: block;
            text-decoration: none;
            color: inherit;
            background: var(--white);
            transition: var(--transition);
            height: 100%;
            border: 1px solid var(--sand);
        }

        .blog-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(0,0,0,0.05);
        }

        .blog-card-image {
            height: 250px;
            overflow: hidden;
        }

        .blog-card-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: var(--transition);
        }

        .blog-card:hover .blog-card-image img {
            transform: scale(1.05);
        }

        .blog-card-content {
            padding: 2rem;
        }

        .blog-card-meta {
            display: flex;
            justify-content: space-between;
            margin-bottom: 1rem;
            font-size: 0.8rem;
            color: var(--grey);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .blog-card-category {
            color: var(--primary);
        }

        .blog-card h3 {
            font-family: 'Playfair Display', serif;
            font-size: 1.3rem;
            font-weight: 400;
            color: var(--dark);
            margin-bottom: 1rem;
            line-height: 1.4;
        }

        .blog-card p {
            font-size: 0.95rem;
            color: var(--grey);
            margin-bottom: 1.5rem;
            line-height: 1.6;
        }

        /* Pagination */
        .pagination-section {
            padding: 3rem 0 0;
        }

        .pagination {
            display: flex;
            justify-content: center;
            gap: 0.5rem;
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .pagination li a,
        .pagination li span {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 45px;
            height: 45px;
            color: var(--grey);
            text-decoration: none;
            border: 1px solid var(--sand);
            transition: var(--transition);
            font-size: 0.95rem;
        }

        .pagination li a:hover {
            background: var(--dark);
            color: var(--white);
            border-color: var(--dark);
        }

        .pagination li.active span {
            background: var(--dark);
            color: var(--white);
            border-color: var(--dark);
        }

        /* Sidebar */
        .sidebar {
            padding-left: 2rem;
        }

        .sidebar-widget {
            margin-bottom: 3rem;
        }

        .sidebar-widget h4 {
            font-family: 'Playfair Display', serif;
            font-size: 1.3rem;
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid var(--sand);
            color: var(--dark);
        }

        .sidebar-about p {
            color: var(--grey);
            line-height: 1.8;
        }

        .popular-posts {
            margin-top: 1rem;
        }

        .popular-post-item {
            display: flex;
            gap: 1rem;
            margin-bottom: 1.5rem;
            padding-bottom: 1.5rem;
            border-bottom: 1px solid var(--sand);
        }

        .popular-post-item:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }

        .popular-post-image {
            width: 80px;
            height: 80px;
            flex-shrink: 0;
        }

        .popular-post-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .popular-post-content h4 {
            font-family: 'Playfair Display', serif;
            font-size: 1rem;
            margin-bottom: 0.5rem;
            line-height: 1.4;
        }

        .popular-post-content h4 a {
            color: var(--dark);
            text-decoration: none;
            transition: var(--transition);
        }

        .popular-post-content h4 a:hover {
            color: var(--primary);
        }

        .popular-post-date {
            font-size: 0.8rem;
            color: var(--grey);
        }

        .category-list {
            list-style: none;
            padding: 0;
        }

        .category-list li {
            margin-bottom: 1rem;
        }

        .category-list li a {
            display: flex;
            justify-content: space-between;
            color: var(--grey);
            text-decoration: none;
            transition: var(--transition);
        }

        .category-list li a:hover {
            color: var(--primary);
            padding-left: 5px;
        }

        .category-list li a span {
            color: var(--sand);
        }

        .tag-cloud {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
        }

        .tag-cloud a {
            display: inline-block;
            padding: 0.3rem 1rem;
            background: var(--light);
            color: var(--grey);
            text-decoration: none;
            font-size: 0.85rem;
            border: 1px solid var(--sand);
            transition: var(--transition);
        }

        .tag-cloud a:hover {
            background: var(--dark);
            color: var(--white);
            border-color: var(--dark);
        }

        /* Newsletter */
        .newsletter-section {
            padding: 5rem 0;
            background: var(--white);
            text-align: center;
        }

        .newsletter-wrapper {
            max-width: 600px;
            margin: 0 auto;
            padding: 3rem;
            background: var(--light);
            border: 1px solid var(--sand);
        }

        .newsletter-wrapper h3 {
            font-family: 'Playfair Display', serif;
            font-size: 2rem;
            font-weight: 400;
            color: var(--dark);
            margin-bottom: 1rem;
        }

        .newsletter-wrapper p {
            color: var(--grey);
            margin-bottom: 2rem;
        }

        .newsletter-form {
            display: flex;
            gap: 1rem;
        }

        .newsletter-form input {
            flex: 1;
            padding: 0.8rem 1rem;
            border: 1px solid var(--sand);
            background: var(--white);
            font-family: 'Inter', sans-serif;
            font-size: 0.95rem;
        }

        .newsletter-form input:focus {
            outline: none;
            border-color: var(--primary);
        }

        .newsletter-form button {
            padding: 0.8rem 2rem;
            background: var(--dark);
            color: var(--white);
            border: 1px solid var(--dark);
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            cursor: pointer;
            transition: var(--transition);
        }

        .newsletter-form button:hover {
            background: transparent;
            color: var(--dark);
        }

        /* No Results */
        .no-results {
            text-align: center;
            padding: 4rem;
            background: var(--white);
            border: 1px solid var(--sand);
        }

        .no-results h3 {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            margin-bottom: 1rem;
            color: var(--dark);
        }

        .no-results p {
            color: var(--grey);
        }

        /* Responsive Design */
        @media (max-width: 992px) {
            .blog-hero h1 {
                font-size: 3rem;
            }
            
            .featured-image {
                height: 350px;
            }
            
            .featured-content {
                padding: 2.5rem;
            }
            
            .featured-content h2 {
                font-size: 2rem;
            }
            
            .sidebar {
                padding-left: 0;
                margin-top: 4rem;
            }
        }

        @media (max-width: 768px) {
            .blog-hero {
                padding: 6rem 0 3rem;
            }
            
            .blog-hero h1 {
                font-size: 2.5rem;
            }
            
            .blog-hero p {
                font-size: 1.1rem;
            }
            
            .featured-image {
                height: 300px;
            }
            
            .featured-content {
                padding: 2rem;
            }
            
            .featured-content h2 {
                font-size: 1.8rem;
            }
            
            .featured-meta {
                gap: 1rem;
                flex-wrap: wrap;
            }
            
            .blog-card-image {
                height: 220px;
            }
            
            .newsletter-form {
                flex-direction: column;
            }
            
            .category-filter {
                gap: 0.5rem;
            }
            
            .category-filter li a {
                padding: 0.4rem 1rem;
                font-size: 0.8rem;
            }
        }

        @media (max-width: 576px) {
            .blog-hero h1 {
                font-size: 2rem;
            }
            
            .blog-hero p {
                font-size: 1rem;
            }
            
            .featured-image {
                height: 250px;
            }
            
            .featured-content {
                padding: 1.5rem;
            }
            
            .featured-content h2 {
                font-size: 1.5rem;
            }
            
            .blog-card-image {
                height: 200px;
            }
            
            .blog-card-content {
                padding: 1.5rem;
            }
            
            .popular-post-item {
                flex-direction: column;
                gap: 0.5rem;
            }
            
            .popular-post-image {
                width: 100%;
                height: 150px;
            }
            
            .newsletter-wrapper {
                padding: 2rem;
            }
            
            .newsletter-wrapper h3 {
                font-size: 1.5rem;
            }
            
            .pagination li a,
            .pagination li span {
                width: 35px;
                height: 35px;
                font-size: 0.85rem;
            }
        }
    </style>
</head>
<body class="quiet-luxury">
    <%- include('partials/header') %>

    <!-- Flash Messages -->
    <% if (messages && messages.error) { %>
        <div class="container mt-4">
            <div class="alert alert-error"><%= messages.error %></div>
        </div>
    <% } %>
    <% if (messages && messages.success) { %>
        <div class="container mt-4">
            <div class="alert alert-success"><%= messages.success %></div>
        </div>
    <% } %>

    <!-- Blog Hero -->
    <section class="blog-hero">
        <div class="container">
            <h1 data-aos="fade-up">Stories & Inspiration</h1>
            <p data-aos="fade-up" data-aos-delay="100">Thoughts on quiet luxury, intentional living, and creating spaces that nurture the soul.</p>
        </div>
    </section>

    <!-- Categories -->
    <section class="blog-categories">
        <div class="container">
            <ul class="category-filter" data-aos="fade-up">
                <li><a href="/blog?category=all" class="<%= !filters || filters.category === 'all' ? 'active' : '' %>">All</a></li>
                <li><a href="/blog?category=interior-design" class="<%= filters && filters.category === 'interior-design' ? 'active' : '' %>">Interior Design</a></li>
                <li><a href="/blog?category=quiet-luxury" class="<%= filters && filters.category === 'quiet-luxury' ? 'active' : '' %>">Quiet Luxury</a></li>
                <li><a href="/blog?category=home-styling" class="<%= filters && filters.category === 'home-styling' ? 'active' : '' %>">Home Styling</a></li>
                <li><a href="/blog?category=behind-the-scenes" class="<%= filters && filters.category === 'behind-the-scenes' ? 'active' : '' %>">Behind the Scenes</a></li>
                <li><a href="/blog?category=client-stories" class="<%= filters && filters.category === 'client-stories' ? 'active' : '' %>">Client Stories</a></li>
            </ul>
        </div>
    </section>

    <!-- Featured Post -->
    <% if (featuredPost) { %>
    <section class="featured-post">
        <div class="container">
            <div class="row g-0">
                <div class="col-lg-6" data-aos="fade-right">
                    <div class="featured-image">
                        <img src="<%= featuredPost.image %>" alt="<%= featuredPost.title %>">
                    </div>
                </div>
                <div class="col-lg-6" data-aos="fade-left">
                    <div class="featured-content">
                        <div class="featured-meta">
                            <span class="featured-category"><%= featuredPost.category %></span>
                            <span class="featured-date"><%= featuredPost.date %></span>
                        </div>
                        <h2><%= featuredPost.title %></h2>
                        <p class="featured-excerpt"><%= featuredPost.excerpt %></p>
                        <a href="/blog/<%= featuredPost.slug %>" class="read-more">Read Article <i class="fas fa-arrow-right"></i></a>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <% } %>

    <!-- Blog Grid with Sidebar -->
    <section class="blog-grid-section">
        <div class="container">
            <div class="row">
                <!-- Main Content -->
                <div class="col-lg-8">
                    <% if (posts && posts.length > 0) { %>
                    <div class="row g-4">
                        <% posts.forEach((post, index) => { %>
                        <div class="col-md-6" data-aos="fade-up" data-aos-delay="<%= index * 50 %>">
                            <a href="/blog/<%= post.slug %>" class="blog-card">
                                <div class="blog-card-image">
                                    <img src="<%= post.image %>" alt="<%= post.title %>">
                                </div>
                                <div class="blog-card-content">
                                    <div class="blog-card-meta">
                                        <span class="blog-card-category"><%= post.category %></span>
                                        <span class="blog-card-date"><%= post.date %></span>
                                    </div>
                                    <h3><%= post.title %></h3>
                                    <p><%= post.excerpt.length > 100 ? post.excerpt.substring(0, 100) + '...' : post.excerpt %></p>
                                    <span class="read-more">Read More <i class="fas fa-arrow-right"></i></span>
                                </div>
                            </a>
                        </div>
                        <% }) %>
                    </div>

                    <!-- Pagination -->
                    <% if (pagination && pagination.totalPages > 1) { %>
                    <div class="pagination-section" data-aos="fade-up">
                        <ul class="pagination">
                            <% if (pagination.hasPrev) { %>
                            <li><a href="/blog?page=<%= pagination.prevPage %><%= filters && filters.category !== 'all' ? '&category=' + filters.category : '' %>"><i class="fas fa-chevron-left"></i></a></li>
                            <% } %>
                            
                            <% for(let i = 1; i <= pagination.totalPages; i++) { %>
                                <% if (i === pagination.page) { %>
                                    <li class="active"><span><%= i %></span></li>
                                <% } else { %>
                                    <li><a href="/blog?page=<%= i %><%= filters && filters.category !== 'all' ? '&category=' + filters.category : '' %>"><%= i %></a></li>
                                <% } %>
                            <% } %>
                            
                            <% if (pagination.hasNext) { %>
                            <li><a href="/blog?page=<%= pagination.nextPage %><%= filters && filters.category !== 'all' ? '&category=' + filters.category : '' %>"><i class="fas fa-chevron-right"></i></a></li>
                            <% } %>
                        </ul>
                    </div>
                    <% } %>
                    
                    <% } else { %>
                    <!-- No Posts Found -->
                    <div class="no-results">
                        <h3>No posts found</h3>
                        <p>Try adjusting your category filter or check back later for new content.</p>
                        <a href="/blog" class="btn btn-outline-dark mt-3">View All Posts</a>
                    </div>
                    <% } %>
                </div>

                <!-- Sidebar -->
                <div class="col-lg-4">
                    <div class="sidebar" data-aos="fade-left">
                        <!-- About -->
                        <div class="sidebar-widget">
                            <h4>About the Blog</h4>
                            <div class="sidebar-about">
                                <img src="/images/blog/studio.jpg" alt="UZYHOMES Studio" class="img-fluid mb-3">
                                <p>Welcome to our journal, where we share insights on quiet luxury, design philosophy, and stories from our projects. Join us on this journey of creating more intentional spaces.</p>
                            </div>
                        </div>

                        <!-- Popular Posts -->
                        <% if (popularPosts && popularPosts.length > 0) { %>
                        <div class="sidebar-widget">
                            <h4>Popular Posts</h4>
                            <div class="popular-posts">
                                <% popularPosts.forEach(post => { %>
                                <div class="popular-post-item">
                                    <div class="popular-post-image">
                                        <img src="<%= post.image %>" alt="<%= post.title %>">
                                    </div>
                                    <div class="popular-post-content">
                                        <h4><a href="/blog/<%= post.slug %>"><%= post.title %></a></h4>
                                        <span class="popular-post-date"><%= post.date %></span>
                                    </div>
                                </div>
                                <% }) %>
                            </div>
                        </div>
                        <% } %>

                        <!-- Categories -->
                        <div class="sidebar-widget">
                            <h4>Categories</h4>
                            <ul class="category-list">
                                <% if (categories && categories.length > 0) { %>
                                    <% categories.forEach(cat => { %>
                                    <li><a href="/blog?category=<%= cat.slug %>"><%= cat.name %> <span>(<%= cat.count %>)</span></a></li>
                                    <% }) %>
                                <% } else { %>
                                    <li class="text-muted">No categories yet</li>
                                <% } %>
                            </ul>
                        </div>

                        <!-- Tags -->
                        <div class="sidebar-widget">
                            <h4>Tags</h4>
                            <div class="tag-cloud">
                                <% if (tags && tags.length > 0) { %>
                                    <% tags.forEach(tag => { %>
                                    <a href="/blog/tag/<%= tag.replace(/ /g, '-') %>"><%= tag %></a>
                                    <% }) %>
                                <% } else { %>
                                    <p class="text-muted">No tags yet</p>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Newsletter -->
    <section class="newsletter-section">
        <div class="container">
            <div class="newsletter-wrapper" data-aos="fade-up">
                <h3>Subscribe to Our Journal</h3>
                <p>Get inspiration and insights delivered to your inbox.</p>
                <form class="newsletter-form" action="/blog/newsletter/subscribe" method="POST">
                    <input type="email" name="email" placeholder="Your email address" required>
                    <button type="submit">Subscribe</button>
                </form>
            </div>
        </div>
    </section>

    <%- include('partials/footer') %>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script src="/js/auth.js"></script>
    <script src="/js/cart.js"></script>
    
    <script>
        AOS.init({
            duration: 800,
            once: true,
            offset: 100
        });
    </script>
</body>
</html>