<!-- views/account.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Account — UZYHOMES</title>
    
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@300;400;500;600&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        /* Account Page Specific Styles - Matching Admin Panel Structure */
        :root {
            --sidebar-width: 280px;
            --header-height: 70px;
            --accent-gold: #b8a48c;
            --bg-light: #faf9f7;
            --border-light: rgba(0, 0, 0, 0.05);
            --text-primary: #1a1a1a;
            --text-secondary: #6c757d;
        }

        body {
            background: var(--bg-light);
            font-family: 'Inter', sans-serif;
        }

        /* Account Wrapper - Like Admin Wrapper */
        .account-wrapper {
            display: flex;
            min-height: 100vh;
            padding-top: var(--header-height);
        }

        /* Sidebar - Like Admin Sidebar */
        .account-sidebar {
            width: var(--sidebar-width);
            background: white;
            border-right: 1px solid var(--border-light);
            position: fixed;
            top: var(--header-height);
            left: 0;
            bottom: 0;
            overflow-y: auto;
            box-shadow: 2px 0 20px rgba(0, 0, 0, 0.02);
            z-index: 100;
        }

        .sidebar-header {
            padding: 30px 24px 20px;
            border-bottom: 1px solid var(--border-light);
        }

        .user-profile {
            text-align: center;
        }

        .user-avatar-large {
            width: 100px;
            height: 100px;
            margin: 0 auto 20px;
            background: #f5f3f0;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 2px solid var(--accent-gold);
        }

        .user-avatar-large i {
            font-size: 3.5rem;
            color: var(--accent-gold);
        }

        .user-name {
            font-family: 'Playfair Display', serif;
            font-size: 1.3rem;
            font-weight: 500;
            color: var(--text-primary);
            margin-bottom: 5px;
        }

        .user-email {
            font-size: 0.9rem;
            color: var(--text-secondary);
            margin-bottom: 15px;
        }

        .user-badge {
            display: inline-block;
            padding: 4px 12px;
            background: var(--accent-gold);
            color: white;
            font-size: 0.7rem;
            letter-spacing: 0.5px;
            text-transform: uppercase;
        }

        .sidebar-nav {
            padding: 20px 0;
        }

        .nav-section {
            margin-bottom: 20px;
        }

        .nav-section-title {
            padding: 8px 24px;
            font-size: 0.7rem;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: var(--text-secondary);
        }

        .nav-item {
            margin: 2px 12px;
        }

        .nav-link {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px 16px;
            border-radius: 0;
            color: var(--text-secondary);
            font-size: 0.9rem;
            transition: all 0.3s ease;
            position: relative;
        }

        .nav-link i {
            width: 20px;
            color: var(--accent-gold);
            transition: all 0.3s ease;
        }

        .nav-link:hover {
            background: var(--bg-light);
            color: var(--text-primary);
        }

        .nav-link:hover i {
            color: var(--text-primary);
        }

        .nav-link.active {
            background: var(--bg-light);
            color: var(--text-primary);
            font-weight: 500;
        }

        .nav-link.active::before {
            content: '';
            position: absolute;
            left: 0;
            top: 20%;
            bottom: 20%;
            width: 3px;
            background: var(--accent-gold);
        }

        .nav-link.active i {
            color: var(--accent-gold);
        }

        .nav-badge {
            margin-left: auto;
            background: var(--accent-gold);
            color: white;
            font-size: 0.65rem;
            padding: 2px 6px;
            border-radius: 10px;
        }

        /* Main Content - Like Admin Main */
        .account-main {
            flex: 1;
            margin-left: var(--sidebar-width);
            padding: 30px;
            background: var(--bg-light);
            min-height: calc(100vh - var(--header-height));
        }

        .account-content {
            max-width: 1200px;
            margin: 0 auto;
        }

        /* Page Header */
        .page-header {
            margin-bottom: 30px;
        }

        .page-title {
            font-family: 'Playfair Display', serif;
            font-size: 2rem;
            font-weight: 400;
            color: var(--text-primary);
            margin: 0;
        }

        .page-subtitle {
            color: var(--text-secondary);
            font-size: 0.95rem;
            margin-top: 5px;
        }

        /* Cards */
        .card {
            background: white;
            border: 1px solid var(--border-light);
            border-radius: 0;
            margin-bottom: 24px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.02);
        }

        .card-header {
            background: white;
            border-bottom: 1px solid var(--border-light);
            padding: 20px 24px;
        }

        .card-header h5 {
            font-family: 'Playfair Display', serif;
            font-size: 1.1rem;
            font-weight: 500;
            color: var(--text-primary);
            margin: 0;
        }

        .card-body {
            padding: 24px;
        }

        /* Stats Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: white;
            border: 1px solid var(--border-light);
            padding: 24px;
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .stat-icon {
            width: 50px;
            height: 50px;
            background: var(--bg-light);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.3rem;
            color: var(--accent-gold);
        }

        .stat-content {
            flex: 1;
        }

        .stat-label {
            font-size: 0.8rem;
            color: var(--text-secondary);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 4px;
        }

        .stat-value {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            font-weight: 500;
            color: var(--text-primary);
            line-height: 1;
        }

        /* Tables */
        .table-wrapper {
            overflow-x: auto;
        }

        .account-table {
            width: 100%;
            border-collapse: collapse;
        }

        .account-table th {
            text-align: left;
            padding: 16px 20px;
            font-size: 0.75rem;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: var(--text-secondary);
            border-bottom: 1px solid var(--border-light);
            background: var(--bg-light);
        }

        .account-table td {
            padding: 16px 20px;
            border-bottom: 1px solid var(--border-light);
            color: var(--text-primary);
            font-size: 0.9rem;
        }

        .account-table tbody tr:hover {
            background: var(--bg-light);
        }

        /* Status Badges */
        .badge-status {
            display: inline-block;
            padding: 4px 12px;
            font-size: 0.7rem;
            font-weight: 400;
            letter-spacing: 0.3px;
        }

        .badge-pending {
            background: #fff3e0;
            color: #b85e00;
            border: 1px solid #ffe4cc;
        }

        .badge-processing {
            background: #e3f2fd;
            color: #0d47a1;
            border: 1px solid #bbdefb;
        }

        .badge-shipped {
            background: #e8f5e9;
            color: #1b5e20;
            border: 1px solid #c8e6c9;
        }

        .badge-delivered {
            background: #e0f2f1;
            color: #004d40;
            border: 1px solid #b2dfdb;
        }

        .badge-cancelled {
            background: #ffebee;
            color: #b71c1c;
            border: 1px solid #ffcdd2;
        }

        .badge-success {
            background: #e8f5e9;
            color: #1b5e20;
            border: 1px solid #a5d6a7;
        }

        .badge-failed {
            background: #ffebee;
            color: #b71c1c;
            border: 1px solid #ffcdd2;
        }

        .badge-refunded {
            background: #e0f2f1;
            color: #006064;
            border: 1px solid #b2ebf2;
        }

        /* Forms */
        .form-group {
            margin-bottom: 20px;
        }

        .form-label {
            display: block;
            font-size: 0.85rem;
            font-weight: 400;
            color: var(--text-secondary);
            margin-bottom: 8px;
        }

        .form-control {
            width: 100%;
            border: 1px solid var(--border-light);
            border-radius: 0;
            padding: 12px 16px;
            font-size: 0.95rem;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--accent-gold);
            box-shadow: 0 0 0 3px rgba(184, 164, 140, 0.1);
        }

        .form-control[readonly] {
            background: var(--bg-light);
            cursor: not-allowed;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }

        /* Buttons */
        .btn {
            padding: 12px 24px;
            font-size: 0.9rem;
            font-weight: 400;
            letter-spacing: 0.5px;
            border-radius: 0;
            transition: all 0.3s ease;
        }

        .btn-primary {
            background: var(--text-primary);
            color: white;
            border: none;
        }

        .btn-primary:hover {
            background: #333;
            transform: translateY(-1px);
        }

        .btn-outline {
            background: transparent;
            border: 1px solid var(--border-light);
            color: var(--text-primary);
        }

        .btn-outline:hover {
            background: var(--text-primary);
            color: white;
            border-color: var(--text-primary);
        }

        .btn-sm {
            padding: 8px 16px;
            font-size: 0.85rem;
        }

        .btn-icon {
            width: 36px;
            height: 36px;
            padding: 0;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }

        /* Address Cards */
        .address-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 20px;
        }

        .address-card {
            background: white;
            border: 1px solid var(--border-light);
            padding: 20px;
            position: relative;
        }

        .address-card.default {
            border-color: var(--accent-gold);
        }

        .default-badge {
            position: absolute;
            top: 10px;
            right: 10px;
            background: var(--accent-gold);
            color: white;
            padding: 4px 12px;
            font-size: 0.7rem;
            letter-spacing: 0.5px;
        }

        .address-name {
            font-family: 'Playfair Display', serif;
            font-size: 1rem;
            font-weight: 500;
            margin-bottom: 10px;
            padding-right: 60px;
        }

        .address-detail {
            font-size: 0.9rem;
            color: var(--text-secondary);
            margin-bottom: 5px;
        }

        .address-actions {
            margin-top: 15px;
            display: flex;
            gap: 10px;
        }

        /* Wishlist Items */
        .wishlist-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 20px;
        }

        .wishlist-item {
            background: white;
            border: 1px solid var(--border-light);
            padding: 15px;
            text-align: center;
        }

        .wishlist-image {
            width: 100%;
            height: 150px;
            object-fit: cover;
            margin-bottom: 15px;
        }

        .wishlist-title {
            font-size: 0.95rem;
            font-weight: 500;
            margin-bottom: 5px;
        }

        .wishlist-price {
            color: var(--accent-gold);
            font-weight: 500;
            margin-bottom: 10px;
        }

        /* Alerts */
        .alert {
            border-radius: 0;
            border: none;
            padding: 16px 20px;
            margin-bottom: 20px;
            border-left: 3px solid transparent;
        }

        .alert-success {
            background: #e8f5e9;
            color: #1b5e20;
            border-left-color: #2e7d32;
        }

        .alert-danger {
            background: #ffebee;
            color: #b71c1c;
            border-left-color: #c62828;
        }

        .alert-info {
            background: #e3f2fd;
            color: #0d47a1;
            border-left-color: #1565c0;
        }

        /* Empty States */
        .empty-state {
            text-align: center;
            padding: 60px 20px;
        }

        .empty-icon {
            font-size: 3rem;
            color: var(--accent-gold);
            margin-bottom: 20px;
            opacity: 0.5;
        }

        .empty-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.2rem;
            color: var(--text-primary);
            margin-bottom: 10px;
        }

        .empty-text {
            color: var(--text-secondary);
            font-size: 0.9rem;
            margin-bottom: 20px;
        }

        /* Pagination */
        .pagination-wrap {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 20px 0;
            border-top: 1px solid var(--border-light);
        }

        .pagination-info {
            font-size: 0.85rem;
            color: var(--text-secondary);
        }

        .pagination {
            display: flex;
            gap: 5px;
        }

        .page-btn {
            width: 36px;
            height: 36px;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 1px solid var(--border-light);
            background: white;
            color: var(--text-secondary);
            font-size: 0.85rem;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .page-btn:hover {
            border-color: var(--accent-gold);
            color: var(--accent-gold);
        }

        .page-btn.active {
            background: var(--text-primary);
            color: white;
            border-color: var(--text-primary);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .account-sidebar {
                transform: translateX(-100%);
                transition: transform 0.3s ease;
            }

            .account-sidebar.open {
                transform: translateX(0);
            }

            .account-main {
                margin-left: 0;
                padding: 20px;
            }

            .form-row {
                grid-template-columns: 1fr;
            }

            .stats-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>

    <%- include('partials/header') %>

    <!-- Payment Status Messages -->
    <% if (locals.payment_status === 'success' || query?.payment === 'success') { %>
        <div class="container mt-4">
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="fas fa-check-circle me-2"></i>
                <strong>Payment Successful!</strong> Your order has been placed and payment confirmed.
                <% if (locals.order_id || query?.order) { %>
                    <a href="/orders/<%= locals.order_id || query?.order %>" class="alert-link ms-2">View Order</a>
                <% } %>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        </div>
    <% } else if (locals.payment_status === 'failed' || query?.payment === 'failed') { %>
        <div class="container mt-4">
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="fas fa-exclamation-circle me-2"></i>
                <strong>Payment Failed</strong>
                <%= locals.error ? decodeURIComponent(locals.error) : (query?.error ? decodeURIComponent(query.error) : 'There was an issue with your payment. Please try again.') %>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        </div>
    <% } %>

    <div class="account-wrapper">
        <!-- Sidebar -->
        <aside class="account-sidebar" id="accountSidebar">
            <div class="sidebar-header">
                <div class="user-profile">
                    <div class="user-avatar-large">
                        <i class="fas fa-user-circle"></i>
                    </div>
                    <div class="user-name" id="sidebarUserName">Loading...</div>
                    <div class="user-email" id="sidebarUserEmail"></div>
                    <div class="user-badge" id="userMemberSince">Member</div>
                </div>
            </div>

            <nav class="sidebar-nav">
                <div class="nav-section">
                    <div class="nav-section-title">Main</div>
                    <div class="nav-item">
                        <a href="#" class="nav-link active" onclick="switchTab(event, 'dashboard')" data-tab="dashboard">
                            <i class="fas fa-home"></i>
                            <span>Dashboard</span>
                        </a>
                    </div>
                    <div class="nav-item">
                        <a href="#" class="nav-link" onclick="switchTab(event, 'orders')" data-tab="orders">
                            <i class="fas fa-shopping-bag"></i>
                            <span>Orders</span>
                            <span class="nav-badge" id="orderCount">0</span>
                        </a>
                    </div>
                </div>

                <div class="nav-section">
                    <div class="nav-section-title">Account</div>
                    <div class="nav-item">
                        <a href="#" class="nav-link" onclick="switchTab(event, 'profile')" data-tab="profile">
                            <i class="fas fa-user"></i>
                            <span>Profile</span>
                        </a>
                    </div>
                    <div class="nav-item">
                        <a href="#" class="nav-link" onclick="switchTab(event, 'addresses')" data-tab="addresses">
                            <i class="fas fa-map-marker-alt"></i>
                            <span>Addresses</span>
                        </a>
                    </div>
                    <div class="nav-item">
                        <a href="#" class="nav-link" onclick="switchTab(event, 'wishlist')" data-tab="wishlist">
                            <i class="fas fa-heart"></i>
                            <span>Wishlist</span>
                        </a>
                    </div>
                </div>

                <div class="nav-section">
                    <div class="nav-section-title">Transactions</div>
                    <div class="nav-item">
                        <a href="#" class="nav-link" onclick="switchTab(event, 'transactions')" data-tab="transactions">
                            <i class="fas fa-credit-card"></i>
                            <span>Transactions</span>
                            <span class="nav-badge" id="transactionCount">0</span>
                        </a>
                    </div>
                </div>

                <div class="nav-section">
                    <div class="nav-section-title">Settings</div>
                    <div class="nav-item">
                        <a href="#" class="nav-link" onclick="switchTab(event, 'password')" data-tab="password">
                            <i class="fas fa-lock"></i>
                            <span>Security</span>
                        </a>
                    </div>
                    <div class="nav-item">
                        <a href="#" class="nav-link" onclick="logout()">
                            <i class="fas fa-sign-out-alt"></i>
                            <span>Logout</span>
                        </a>
                    </div>
                </div>
            </nav>
        </aside>

        <!-- Main Content -->
        <main class="account-main">
            <div class="account-content">
                <!-- Dashboard Tab -->
                <div id="dashboard-tab" class="tab-pane active">
                    <div class="page-header">
                        <h1 class="page-title">Dashboard</h1>
                        <p class="page-subtitle">Welcome back, <span id="dashboardName"></span></p>
                    </div>

                    <!-- Stats Cards -->
                    <div class="stats-grid">
                        <div class="stat-card">
                            <div class="stat-icon">
                                <i class="fas fa-shopping-bag"></i>
                            </div>
                            <div class="stat-content">
                                <div class="stat-label">Total Orders</div>
                                <div class="stat-value" id="totalOrders">0</div>
                            </div>
                        </div>
                        <div class="stat-card">
                            <div class="stat-icon">
                                <i class="fas fa-clock"></i>
                            </div>
                            <div class="stat-content">
                                <div class="stat-label">Pending Orders</div>
                                <div class="stat-value" id="pendingOrders">0</div>
                            </div>
                        </div>
                        <div class="stat-card">
                            <div class="stat-icon">
                                <i class="fas fa-check-circle"></i>
                            </div>
                            <div class="stat-content">
                                <div class="stat-label">Completed</div>
                                <div class="stat-value" id="completedOrders">0</div>
                            </div>
                        </div>
                        <div class="stat-card">
                            <div class="stat-icon">
                                <i class="fas fa-heart"></i>
                            </div>
                            <div class="stat-content">
                                <div class="stat-label">Wishlist</div>
                                <div class="stat-value" id="wishlistCount">0</div>
                            </div>
                        </div>
                    </div>

                    <!-- Recent Orders -->
                    <div class="card">
                        <div class="card-header d-flex justify-content-between align-items-center">
                            <h5>Recent Orders</h5>
                            <a href="#" class="btn btn-outline btn-sm" onclick="switchTab(event, 'orders')">View All</a>
                        </div>
                        <div class="card-body">
                            <div class="table-wrapper">
                                <table class="account-table">
                                    <thead>
                                        <tr>
                                            <th>Order #</th>
                                            <th>Date</th>
                                            <th>Total</th>
                                            <th>Status</th>
                                            <th>Payment</th>
                                            <th></th>
                                        </tr>
                                    </thead>
                                    <tbody id="recentOrdersTable">
                                        <tr>
                                            <td colspan="6" class="text-center py-4">
                                                <i class="fas fa-spinner fa-spin me-2"></i>Loading orders...
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>

                    <!-- Recent Transactions -->
                    <div class="card mt-4">
                        <div class="card-header d-flex justify-content-between align-items-center">
                            <h5>Recent Transactions</h5>
                            <a href="#" class="btn btn-outline btn-sm" onclick="switchTab(event, 'transactions')">View All</a>
                        </div>
                        <div class="card-body">
                            <div class="table-wrapper">
                                <table class="account-table">
                                    <thead>
                                        <tr>
                                            <th>Reference</th>
                                            <th>Date</th>
                                            <th>Amount</th>
                                            <th>Method</th>
                                            <th>Status</th>
                                        </tr>
                                    </thead>
                                    <tbody id="recentTransactionsTable">
                                        <tr>
                                            <td colspan="5" class="text-center py-4">
                                                <i class="fas fa-spinner fa-spin me-2"></i>Loading transactions...
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Orders Tab -->
                <div id="orders-tab" class="tab-pane" style="display: none;">
                    <div class="page-header d-flex justify-content-between align-items-center">
                        <div>
                            <h1 class="page-title">Orders</h1>
                            <p class="page-subtitle">Track and manage your orders</p>
                        </div>
                    </div>

                    <!-- Filter Bar -->
                    <div class="filter-bar mb-4" style="display: flex; gap: 10px;">
                        <select class="form-control" style="width: auto; min-width: 150px;" id="orderStatusFilter">
                            <option value="">All Status</option>
                            <option value="pending">Pending</option>
                            <option value="processing">Processing</option>
                            <option value="shipped">Shipped</option>
                            <option value="delivered">Delivered</option>
                            <option value="cancelled">Cancelled</option>
                        </select>
                        <input type="date" class="form-control" style="width: auto;" id="orderDateFilter">
                    </div>

                    <!-- Orders Table -->
                    <div class="card">
                        <div class="table-wrapper">
                            <table class="account-table">
                                <thead>
                                    <tr>
                                        <th>Order #</th>
                                        <th>Date</th>
                                        <th>Items</th>
                                        <th>Total</th>
                                        <th>Status</th>
                                        <th>Payment</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="ordersTable">
                                    <tr>
                                        <td colspan="7" class="text-center py-4">
                                            <i class="fas fa-spinner fa-spin me-2"></i>Loading orders...
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                        <div class="pagination-wrap" id="ordersPagination">
                            <div class="pagination-info"></div>
                            <div class="pagination"></div>
                        </div>
                    </div>
                </div>

                <!-- Transactions Tab -->
                <div id="transactions-tab" class="tab-pane" style="display: none;">
                    <div class="page-header d-flex justify-content-between align-items-center">
                        <div>
                            <h1 class="page-title">Transactions</h1>
                            <p class="page-subtitle">View your payment history</p>
                        </div>
                    </div>

                    <!-- Transaction Stats -->
                    <div class="stats-grid">
                        <div class="stat-card">
                            <div class="stat-icon">
                                <i class="fas fa-credit-card"></i>
                            </div>
                            <div class="stat-content">
                                <div class="stat-label">Total Spent</div>
                                <div class="stat-value" id="totalSpent">₦0</div>
                            </div>
                        </div>
                        <div class="stat-card">
                            <div class="stat-icon">
                                <i class="fas fa-check-circle"></i>
                            </div>
                            <div class="stat-content">
                                <div class="stat-label">Successful</div>
                                <div class="stat-value" id="successfulTransactions">0</div>
                            </div>
                        </div>
                        <div class="stat-card">
                            <div class="stat-icon">
                                <i class="fas fa-clock"></i>
                            </div>
                            <div class="stat-content">
                                <div class="stat-label">Pending</div>
                                <div class="stat-value" id="pendingTransactions">0</div>
                            </div>
                        </div>
                        <div class="stat-card">
                            <div class="stat-icon">
                                <i class="fas fa-undo-alt"></i>
                            </div>
                            <div class="stat-content">
                                <div class="stat-label">Refunded</div>
                                <div class="stat-value" id="refundedTransactions">0</div>
                            </div>
                        </div>
                    </div>

                    <!-- Filter Bar -->
                    <div class="filter-bar mb-4" style="display: flex; gap: 10px;">
                        <select class="form-control" style="width: auto; min-width: 150px;" id="transactionStatusFilter">
                            <option value="">All Status</option>
                            <option value="completed">Completed</option>
                            <option value="pending">Pending</option>
                            <option value="failed">Failed</option>
                            <option value="refunded">Refunded</option>
                        </select>
                        <select class="form-control" style="width: auto; min-width: 150px;" id="transactionMethodFilter">
                            <option value="">All Methods</option>
                            <option value="card">Card</option>
                            <option value="transfer">Transfer</option>
                            <option value="paystack">Paystack</option>
                        </select>
                        <input type="date" class="form-control" style="width: auto;" id="transactionDateFilter">
                    </div>

                    <!-- Transactions Table -->
                    <div class="card">
                        <div class="table-wrapper">
                            <table class="account-table">
                                <thead>
                                    <tr>
                                        <th>Reference</th>
                                        <th>Date</th>
                                        <th>Order #</th>
                                        <th>Amount</th>
                                        <th>Method</th>
                                        <th>Status</th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody id="transactionsTable">
                                    <tr>
                                        <td colspan="7" class="text-center py-4">
                                            <i class="fas fa-spinner fa-spin me-2"></i>Loading transactions...
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                        <div class="pagination-wrap" id="transactionsPagination">
                            <div class="pagination-info"></div>
                            <div class="pagination"></div>
                        </div>
                    </div>
                </div>

                <!-- Profile Tab -->
                <div id="profile-tab" class="tab-pane" style="display: none;">
                    <div class="page-header">
                        <h1 class="page-title">Profile Settings</h1>
                        <p class="page-subtitle">Manage your personal information</p>
                    </div>

                    <div class="card">
                        <div class="card-header">
                            <h5>Personal Information</h5>
                        </div>
                        <div class="card-body">
                            <div id="profileAlert" style="display: none;"></div>
                            
                            <form id="profileForm">
                                <div class="form-row">
                                    <div class="form-group">
                                        <label class="form-label">First Name</label>
                                        <input type="text" class="form-control" id="profileFirstName" required>
                                    </div>
                                    <div class="form-group">
                                        <label class="form-label">Last Name</label>
                                        <input type="text" class="form-control" id="profileLastName" required>
                                    </div>
                                </div>
                                
                                <div class="form-group">
                                    <label class="form-label">Email Address</label>
                                    <input type="email" class="form-control" id="profileEmail" readonly>
                                </div>
                                
                                <div class="form-group">
                                    <label class="form-label">Phone Number</label>
                                    <input type="tel" class="form-control" id="profilePhone">
                                </div>
                                
                                <button type="submit" class="btn btn-primary" id="updateProfileBtn">
                                    <span id="updateProfileText">Update Profile</span>
                                    <span id="updateProfileSpinner" style="display: none;">
                                        <span class="spinner-border spinner-border-sm"></span> Updating...
                                    </span>
                                </button>
                            </form>
                        </div>
                    </div>
                </div>

                <!-- Security Tab -->
                <div id="password-tab" class="tab-pane" style="display: none;">
                    <div class="page-header">
                        <h1 class="page-title">Security</h1>
                        <p class="page-subtitle">Change your password</p>
                    </div>

                    <div class="card">
                        <div class="card-header">
                            <h5>Change Password</h5>
                        </div>
                        <div class="card-body">
                            <div id="passwordAlert" style="display: none;"></div>
                            
                            <form id="changePasswordForm">
                                <div class="form-group">
                                    <label class="form-label">Current Password</label>
                                    <input type="password" class="form-control" id="currentPassword" required>
                                </div>
                                
                                <div class="form-row">
                                    <div class="form-group">
                                        <label class="form-label">New Password</label>
                                        <input type="password" class="form-control" id="newPassword" required>
                                    </div>
                                    <div class="form-group">
                                        <label class="form-label">Confirm New Password</label>
                                        <input type="password" class="form-control" id="confirmNewPassword" required>
                                    </div>
                                </div>
                                
                                <button type="submit" class="btn btn-primary" id="changePasswordBtn">
                                    <span id="changePasswordText">Change Password</span>
                                    <span id="changePasswordSpinner" style="display: none;">
                                        <span class="spinner-border spinner-border-sm"></span> Updating...
                                    </span>
                                </button>
                            </form>
                        </div>
                    </div>
                </div>

                <!-- Addresses Tab -->
                <div id="addresses-tab" class="tab-pane" style="display: none;">
                    <div class="page-header d-flex justify-content-between align-items-center">
                        <div>
                            <h1 class="page-title">Addresses</h1>
                            <p class="page-subtitle">Manage your shipping addresses</p>
                        </div>
                        <button class="btn btn-primary" onclick="showAddAddressModal()">
                            <i class="fas fa-plus me-2"></i>Add New Address
                        </button>
                    </div>

                    <div class="address-grid" id="addressesList">
                        <div class="text-center py-5" style="grid-column: 1/-1;">
                            <i class="fas fa-spinner fa-spin me-2"></i>Loading addresses...
                        </div>
                    </div>
                </div>

                <!-- Wishlist Tab -->
                <div id="wishlist-tab" class="tab-pane" style="display: none;">
                    <div class="page-header">
                        <h1 class="page-title">Wishlist</h1>
                        <p class="page-subtitle">Your saved items</p>
                    </div>

                    <div class="wishlist-grid" id="wishlistItems">
                        <div class="text-center py-5" style="grid-column: 1/-1;">
                            <i class="fas fa-spinner fa-spin me-2"></i>Loading wishlist...
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <%- include('partials/footer') %>

    <!-- Add Address Modal -->
    <div class="modal fade" id="addAddressModal" tabindex="-1">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Add New Address</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <form id="addAddressForm">
                        <div class="form-row">
                            <div class="form-group">
                                <label class="form-label">First Name</label>
                                <input type="text" class="form-control" id="addressFirstName" required>
                            </div>
                            <div class="form-group">
                                <label class="form-label">Last Name</label>
                                <input type="text" class="form-control" id="addressLastName" required>
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <label class="form-label">Street Address</label>
                            <input type="text" class="form-control" id="addressStreet" required>
                        </div>
                        
                        <div class="form-row">
                            <div class="form-group">
                                <label class="form-label">City</label>
                                <input type="text" class="form-control" id="addressCity" required>
                            </div>
                            <div class="form-group">
                                <label class="form-label">State</label>
                                <input type="text" class="form-control" id="addressState" required>
                            </div>
                            <div class="form-group">
                                <label class="form-label">ZIP Code</label>
                                <input type="text" class="form-control" id="addressZip" required>
                            </div>
                        </div>
                        
                        <div class="form-row">
                            <div class="form-group">
                                <label class="form-label">Country</label>
                                <input type="text" class="form-control" id="addressCountry" value="Nigeria" required>
                            </div>
                            <div class="form-group">
                                <label class="form-label">Phone Number</label>
                                <input type="tel" class="form-control" id="addressPhone" required>
                            </div>
                        </div>
                        
                        <div class="form-check">
                            <input type="checkbox" class="form-check-input" id="setDefaultAddress">
                            <label class="form-check-label">Set as default address</label>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" class="btn btn-primary" onclick="saveNewAddress()">Save Address</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Edit Address Modal -->
    <div class="modal fade" id="editAddressModal" tabindex="-1">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Edit Address</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <form id="editAddressForm">
                        <input type="hidden" id="editAddressId">
                        <div class="form-row">
                            <div class="form-group">
                                <label class="form-label">First Name</label>
                                <input type="text" class="form-control" id="editAddressFirstName" required>
                            </div>
                            <div class="form-group">
                                <label class="form-label">Last Name</label>
                                <input type="text" class="form-control" id="editAddressLastName" required>
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <label class="form-label">Street Address</label>
                            <input type="text" class="form-control" id="editAddressStreet" required>
                        </div>
                        
                        <div class="form-row">
                            <div class="form-group">
                                <label class="form-label">City</label>
                                <input type="text" class="form-control" id="editAddressCity" required>
                            </div>
                            <div class="form-group">
                                <label class="form-label">State</label>
                                <input type="text" class="form-control" id="editAddressState" required>
                            </div>
                            <div class="form-group">
                                <label class="form-label">ZIP Code</label>
                                <input type="text" class="form-control" id="editAddressZip" required>
                            </div>
                        </div>
                        
                        <div class="form-row">
                            <div class="form-group">
                                <label class="form-label">Country</label>
                                <input type="text" class="form-control" id="editAddressCountry" required>
                            </div>
                            <div class="form-group">
                                <label class="form-label">Phone Number</label>
                                <input type="tel" class="form-control" id="editAddressPhone" required>
                            </div>
                        </div>
                        
                        <div class="form-check">
                            <input type="checkbox" class="form-check-input" id="editSetDefaultAddress">
                            <label class="form-check-label">Set as default address</label>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" class="btn btn-primary" onclick="updateAddress()">Update Address</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Custom JS -->
    <script src="/js/auth.js"></script>
    <script src="/js/cart.js"></script>
    <script src="/js/account.js"></script>
    
    <script>
        // Tab switching function
        function switchTab(event, tabName) {
            event.preventDefault();
            
            // Hide all tabs
            document.querySelectorAll('.tab-pane').forEach(tab => {
                tab.style.display = 'none';
            });
            
            // Show selected tab
            document.getElementById(tabName + '-tab').style.display = 'block';
            
            // Update active state in sidebar
            document.querySelectorAll('.nav-link').forEach(link => {
                link.classList.remove('active');
            });
            
            event.currentTarget.classList.add('active');
            
            // Load tab-specific data
            if (tabName === 'orders') loadOrders();
            if (tabName === 'transactions') loadTransactions();
            if (tabName === 'addresses') loadAddresses();
            if (tabName === 'wishlist') loadWishlist();
        }

        // Auto-dismiss alerts
        document.addEventListener('DOMContentLoaded', function() {
            setTimeout(() => {
                document.querySelectorAll('.alert').forEach(alert => {
                    const bsAlert = new bootstrap.Alert(alert);
                    bsAlert.close();
                });
            }, 5000);
        });

    
    </script>
</body>
</html>